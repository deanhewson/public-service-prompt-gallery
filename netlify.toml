[build]
  command = "npm run build"
  publish = "dist"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- . ':(exclude)*.md' ':(exclude)notes/**' ':(exclude)scripts/**'"

[build.environment]
  NODE_VERSION = "20"
